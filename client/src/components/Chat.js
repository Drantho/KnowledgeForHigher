import { React, useState, useEffect } from 'react';


// Expected props:
//      id: user's chat ID
export default function Chat(props) {
    useEffect( () => {
        // If user ID does not exist, create one and save it to local storage
        // If token does not exist, create one
        //      If token does exist, refresh it and save it to local storage
        // Generate chat client object
        // Get list of thread IDs
    });
    return (
        <div>
            
        </div>
    )
}
